#!/usr/bin/env python

from distutils.core import setup
import glob


setup(name='invoice_db',
      version='1.0',
      description='',
      author='Maya Brandi',
      author_email='maya.brandi@scilifelab.se',
      packages=['clinicaladmin'],
#      scripts=glob.glob("database.py")
     )
